export const DEBUG = true;

// Default values for OpenAI Completion parameters
export const DEFAULT_MODEL = 'text-davinci-003';
export const DEFAULT_MAX_TOKENS = 150;
export const DEFAULT_TEMPERATURE = 0.7;
export const DEFAULT_TOP_P = 1.0;
export const DEFAULT_FREQUENCY_PENALTY = 0.0;
export const DEFAULT_PRESENCE_PENALTY = 0.0;

// Timeout and progress settings
export const COMPLETION_TIMEOUT_MS = 60000; // 60 seconds
export const PROGRESS_UPDATE_INTERVAL_MS = 500; // 0.5 seconds

// Server info
export const SERVER_NAME = 'openai-complete';
export const SERVER_VERSION = '0.1.0'; 